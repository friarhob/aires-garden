Title: Hello, world
Slug: hello-world
Date: 2026-04-20
Lang: en
Status: published
Tags: meta
Translation_key: hello-world

> **Scaffold demo post — delete when real content exists.**
> This post was created by the `scaffold-pelican-site` OpenSpec change purely to prove the Pelican pipeline works end-to-end (Markdown → theme → HTML → served). It has no editorial value and should be removed before the garden is shared publicly, or as soon as there's a real first post.

First post in the garden. Markdown in `content/posts/` is parsed, passed through the custom theme at `themes/garden/`, and rendered as static HTML in `output/`.

Frontmatter format note: this post uses Pelican's native `Key: value` metadata format rather than YAML. The YAML frontmatter promised in the project baseline lands in the `add-content-model` proposal, which introduces a YAML reader alongside frontmatter schema validation.
